pluginManagement {
    repositories {
        google()
        jcenter()
        gradlePluginPortal()
        mavenCentral()
    }
}

includeBuild("convention-plugins")

rootProject.name = "Kextensions-core"

